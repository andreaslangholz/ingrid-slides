# TODO

## In progress

- [ ] **Decouple into a multi-deck system** — turn the single-file deck into a shared engine + many decks (local-first now, designed to migrate to a git repo or hosted app; keeps HTML-slides + in-browser editor)
  - [x] Phase 1 — extract shared `engine/` (css, js, edit, charts); deck is now a thin shell (PR #1, merged)
  - [x] Phase 2a — first multi-deck use case: split `ingrid.html` into `ingrid_examples.html` (generic gallery) + `ingrid_library.html` (concrete content), both sharing `engine/`
  - [x] Phase 2b — root launcher `index.html` lists decks (open/edit); old marketing page kept as `landing.html`. Decks come from a one-line-per-deck array (lightweight manifest)
  - [ ] Phase 2c (optional, DEFERRED) — `decks/<name>/` folders + manifest. Not needed yet: decks aren't media-heavy (`media/` is just a README; decks share `brand/ingrid/`). Also note `file://` breaks a fetched `decks.json`, so any manifest must be a JS file (`decks.js` / `window.DECKS`), not fetched JSON. Revisit when a deck needs its own media.
  - [x] Phase 3 — slide template library: `templates/templates.js` (single source of truth, 23 on-brand templates) + `templates/gallery.html` (browsable preview, tag filters, Copy HTML) + "＋ Add slide" picker in `engine/edit.js` (lazy-loads templates.js, inserts after current slide via the duplicate path → saves as a NEW SLIDE; uniquifies chart-data ids on repeat insert). Note: chart/diagram previews render structure-only in the gallery/picker (charts.js renders lazily via show()); they render fully once inserted into a live deck.
  - [ ] Phase 4 — git-repo / hosted workflow (manifest + file-writes → API later, engine/editor untouched)
- [ ] **Complete Ingrid MCP integration** — blocked on platform team
  - Get real `env` and `namespace` values (currently placeholders in `.env.example`)
  - Copy `.env.example` → `.env.local` and fill in all four vars
  - Test auth: `source .env.local && source scripts/ingrid-auth.sh`
  - Verify Ingrid tools appear in Claude Code (`site-config__*`, `analytics-mcp__*`)
  - Test a live data pull → write to `data/` CSV → chart slide

## Backlog

- [x] **Design new-presentation starter concept** — delivered as workstream E: three starter decks (`starter-blank.html`, `starter-market-update.html`, `starter-pitch.html`) framed in the launcher under "Start a new deck"
- [ ] **Build object and flowchart visualisation component** — native CSS component (no library) for boxes, labels, and arrows; document in AGENTS.md
- [ ] **Integrate standard Ingrid company slides** — port boilerplate slides from `media/Ingrid.pptx` into reusable HTML templates
- [~] **Incorporate Ingrid story & main messaging as AI-available structures** — scaffold delivered as workstream D (`knowledge/{COMPANY,MESSAGING,PRODUCTS,MARKET,PROOF,GLOSSARY}.md`, wired into AGENTS.md). Still needs the user to fill in the real narrative/messaging content
- [~] **Build the self-contained Ingrid presentation system** — full plan in [`SYSTEM-PLAN.md`](SYSTEM-PLAN.md). Workstreams A–E done; F (collaboration/VC) remains, by design a future step. North star = 7 qualities: (1) final deck = 1 self-contained shareable HTML, (2) on Ingrid brand/design, (3) text anchored in company knowledge/wording, (4) usable by multiple agents, (5) clear human-edit vs agent-generated boundary, (6) shared components + starter decks to pick from, (7) collaboration/VC-ready (future). Core gaps: decks aren't self-contained (thin shells link `engine/` + 16 `brand/` images + CDN → needs a bundler), `knowledge/` absent, and AGENTS.md/DESIGN.md/USING.md still carry upstream-fork tokens + a 33-component reference whose classes don't exist in `engine.css`. Entry stays agent-agnostic (`AGENTS.md`, no `CLAUDE.md`). (supersedes the old "clean up structure to be ai native" line)
  - [x] A — single source of truth + agnostic docs (qual 2,3,4): rewrote `AGENTS.md` as thin agent-agnostic router (stripped upstream tokens + 33-component ref); `scripts/gen-components-doc.mjs` → `docs/COMPONENTS.md` from `templates/templates.js` (23 components); repurposed `docs/DESIGN.md` (craft, tokens by ref to BRAND.md); rewrote `docs/USING.md`; rewrote `README.md` as the Ingrid front door. Upstream artifacts archived to `reference/` (deck*.html, landing.html, DESIGN-upstream.md). Acceptance met: no `slide-inner`/`#f5f5f3`/`Inter` in active docs (only AGENTS.md names them in a "never copy these" rule). The `engine/charts.js` `Inter` gap is now fixed — Chart.js defaults + Mermaid themeVariables use the brand stack (`Plus Jakarta Sans`).
  - [x] B — self-contained export (qual 1): `scripts/bundle.mjs` inlines engine + templates.js + base64-embeds used `brand/` images → `dist/<deck>.html`; `--offline` inlines CDN libs+fonts (woff2); `--no-edit` strips editor; large photos downscaled via `sips`. Default keeps editor + CDN. `dist/` gitignored. Verified self-contained across all three modes.
  - [x] C — division of labor (qual 5): canonical table in `AGENTS.md`; `docs/USING.md` Boundary note links to it (no duplicate). Audited every human-edit row against `engine/edit.js` — all present and correctly documented.
  - [x] D — institutional knowledge (qual 3): `knowledge/` = README + `{COMPANY,MESSAGING,PRODUCTS,MARKET,PROOF,GLOSSARY}.md` as empty fill-in templates (`TODO (user)` markers, no fabricated facts, per the resolved decision); wired into `AGENTS.md` (where-everything-lives table, never-fabricate rule, build-a-deck step 2). User still fills in real facts.
  - [x] E — starter decks (qual 6): `starter-blank.html` (cover→agenda→content→closing), `starter-market-update.html` (stats + chart, inline CSV), `starter-pitch.html` (six-beat arc), framed in the launcher as pick-a-start.
  - [ ] F — collaboration/VC (qual 7, future) + plugin alignment: source/`dist` split conventions; point the `slides` plugin at the same sources (ties into the plugin TODO below); decide fate of upstream artifacts (`deck.html`, `deck-craft/solid.html`, `landing.html`).
- [ ] **Update the `slides` plugin + commands for the Ingrid system** — the bundled plugin (`plugins/slides/`) is still upstream-generic (references `deck.html`/`deck-craft.html`/`deck-solid.html` + the generic 31-component `AGENTS.md`; zero mentions of `ingrid`, `engine/`, or `design-system.html`). Re-point commands/skill at `ingrid_examples.html`/`ingrid_library.html`, the shared `engine/`, and the Ingrid `AGENTS.md`/`design-system.html`; swap the decks bundled in `plugins/slides/skills/deck-system/` for the Ingrid shells. Then install locally via marketplace (`source: ./plugins/slides`) so `/slides`, `/add-slide`, etc. generate on-brand Ingrid decks. NOTE: must `/plugin marketplace add .` before `/plugin install slides` (install alone fails — marketplace not registered yet).

## Done

- [x] **Build Ingrid-specific styleguide deck** — delivered as `design-system.html` (the full Ingrid brand & design system: logo, color, type, gradient, photography, icons, visual system, tokens, components). Covers everything the original `ingrid-styleguide.html` item scoped.
- [x] **Structure Ingrid assets and pictures** — reorganised into `brand/ingrid/` (logos, gradients, photos, video, templates) with consistent naming, duplicates removed, `BRAND.md` index created, `.gitignore` + `AGENTS.md` updated
- [x] **Align brand instructions with the styleguide PDF** — verified colors, gradient stops, typography, photography, iconography, visual system against all 47 pages; generated the two missing Arc marks; fixed swapped/broken logo references and the wrong invert/screen trick in `ingrid.html`
